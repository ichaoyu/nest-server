import { ApiProperty } from '@nestjs/swagger';

import { ENTITY_CONFIG_TYPE } from '@/constants';

import { {{UpName}}Entity } from '../entities';

/**
 * 响应传输对象
 */
export class {{UpName}}EntityVO implements {{UpName}}Entity {
  @ApiProperty({ description: '主键' })
  id: number;

  @ApiProperty({
    description: '系统内置（Y是 N否）',
    enum: ENTITY_CONFIG_TYPE,
  })
  configType: ENTITY_CONFIG_TYPE;

  @ApiProperty({ description: '创建时间' })
  createTime: Date;

  @ApiProperty({ description: '更新时间' })
  updateTime: Date;
}
